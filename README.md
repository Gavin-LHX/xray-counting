# xray-counting

?????? Xray ?????????????????

- ????????????? IP ???
- ?????? IP TOP N
- ????????????????
- ???? Let's Encrypt ????? HTTPS/443

## ????

- `cxc.sh`
  - ????
  - ??????????? IP ???
  - ?????? IP TOP???? 10?
- `count_xray_clients.sh`
  - ?????????
  - ???????????? N ???
- `xray_dashboard.py`
  - Python ???????? Web ??????????
  - ???????15 ??
- `cxc-web.sh`
  - ???????`start|stop|restart|status`
  - ACME ?????`acme`?80 ???
  - ACME DNS ?????`acme-dns`????

## ????

```text
.
??? cxc.sh
??? cxc-web.sh
??? count_xray_clients.sh
??? xray_dashboard.py
```

## ????

- Linux ???
- ?????? Xray
- ??? Xray `access.log`
- Python 3????????

## ?????apt / yum?

??????? `root`???? `sudo`?

### Debian / Ubuntu?apt?

```bash
sudo apt update
sudo apt install -y bash curl wget python3 openssl gawk coreutils procps iproute2
```

### CentOS / Rocky / AlmaLinux?yum / dnf?

```bash
sudo yum install -y bash curl wget python3 openssl gawk coreutils procps-ng iproute
```

?????? `wget`???? `curl` ???

```bash
curl -fsSL -o cxc.sh https://raw.githubusercontent.com/Gavin-LHX/xray-counting/main/cxc.sh
curl -fsSL -o cxc-web.sh https://raw.githubusercontent.com/Gavin-LHX/xray-counting/main/cxc-web.sh
```

???????? certbot ??????

```bash
# Debian/Ubuntu
sudo apt install -y certbot

# CentOS/Rocky/AlmaLinux???????????? EPEL?
sudo yum install -y certbot
```

## ???????????

### 0) ?? raw.githubusercontent.com ????

```bash
wget -O cxc.sh https://raw.githubusercontent.com/Gavin-LHX/xray-counting/main/cxc.sh
wget -O count_xray_clients.sh https://raw.githubusercontent.com/Gavin-LHX/xray-counting/main/count_xray_clients.sh
chmod +x cxc.sh count_xray_clients.sh
```

### 1) ????

```bash
bash cxc.sh
```

?????`/var/log/xray/access.log`

### 2) ??????? TOP ??

```bash
bash cxc.sh /var/log/xray/access.log 10
```

## ??????????

### 0) ?? raw.githubusercontent.com ????

```bash
wget -O xray_dashboard.py https://raw.githubusercontent.com/Gavin-LHX/xray-counting/main/xray_dashboard.py
wget -O cxc-web.sh https://raw.githubusercontent.com/Gavin-LHX/xray-counting/main/cxc-web.sh
chmod +x cxc-web.sh
```

### 1) ?? HTTP ??

```bash
bash cxc-web.sh start
```

?????`0.0.0.0:8080`

???`http://?????IP:8080`

### 2) ?? HTTPS??? 443?

??????

```bash
HTTPS_ENABLE=1 PORT=443 SSL_CERT=/root/fullchain.pem SSL_KEY=/root/privkey.pem bash cxc-web.sh restart
```

???`https://?????IP:443`

### 3) ACME ???? Let's Encrypt ???80 ???

?????????????

```bash
ACME_DOMAIN=???? ACME_EMAIL=???? bash cxc-web.sh acme
```

??????????

```bash
bash cxc-web.sh acme ???? ????
```

???

- ????? `standalone`??? 80 ?????????
- `ACME_TOOL=auto` ??? `acme.sh`??? `certbot`?

### 4) ???ACME DNS ?????? 80 ???

Cloudflare ???`acme.sh` DNS API??

```bash
export CF_Token="??CF API Token"
export CF_Account_ID="??CF Account ID"
ACME_DOMAIN=example.com ACME_EMAIL=you@example.com ACME_DNS_PROVIDER=dns_cf bash cxc-web.sh acme-dns
```

??? DNS ???

```bash
export Ali_Key="??Aliyun AccessKey"
export Ali_Secret="??Aliyun AccessSecret"
ACME_DOMAIN=example.com ACME_EMAIL=you@example.com ACME_DNS_PROVIDER=dns_ali bash cxc-web.sh acme-dns
```

?? DNS ????? `ACME_DNS_PROVIDER`??

```bash
ACME_DOMAIN=example.com ACME_EMAIL=you@example.com bash cxc-web.sh acme-dns
```

????????

```bash
ACME_WILDCARD=1 ACME_DOMAIN=example.com ACME_EMAIL=you@example.com ACME_DNS_PROVIDER=dns_cf bash cxc-web.sh acme-dns
```

DNS ?????

- `acme-dns` ???? `acme.sh`?
- `ACME_DNS_SLEEP` ?? DNS ????????? 120 ???

### 5) ??????

```bash
bash cxc-web.sh status
bash cxc-web.sh restart
bash cxc-web.sh stop
```

## ????

`cxc-web.sh` ???????

- `LOG_FILE`?Xray access ??????? `/var/log/xray/access.log`?
- `PORT`???????? HTTP ? `8080`?HTTPS ? `443`?
- `TOP_N`?TOP ??????? `10`?
- `HTTPS_ENABLE`????? HTTPS?`1` ????? `0`?
- `SSL_CERT`???????? `/root/fullchain.pem`?
- `SSL_KEY`???????? `/root/privkey.pem`?
- `ACME_TOOL`?`auto|acme.sh|certbot`??? `auto`?
- `ACME_DOMAIN`????????
- `ACME_EMAIL`????????????
- `ACME_SERVER`?ACME ?????? `letsencrypt`?
- `ACME_DNS_PROVIDER`?DNS ????? `dns_cf`?`dns_ali`?
- `ACME_DNS_SLEEP`?DNS ????????? `120`?
- `ACME_WILDCARD`?????????`1` ????

???

```bash
PORT=9000 TOP_N=20 bash cxc-web.sh restart
HTTPS_ENABLE=1 PORT=443 SSL_CERT=/root/fullchain.pem SSL_KEY=/root/privkey.pem TOP_N=20 bash cxc-web.sh restart
ACME_TOOL=acme.sh ACME_DOMAIN=example.com ACME_EMAIL=admin@example.com bash cxc-web.sh acme
ACME_DOMAIN=example.com ACME_EMAIL=admin@example.com ACME_DNS_PROVIDER=dns_cf bash cxc-web.sh acme-dns
```

## ????

### ???????? 0?

?????

- `access.log` ????
- Xray ??? access ??
- ??????????????
- ????????????

### ???????????

?????????????? IP ???????????????? NAT ???????????????

## ????

- ?????????SSH ?????????? GitHub?
- ???? SSH ???????? root ???????
- ???????????????????????????

## License

MIT
